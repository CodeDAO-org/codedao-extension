name: Build VS Code Extension
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '16'
        
    - name: Install vsce only
      run: npm install -g vsce@2.15.0
      
    - name: Package extension
      run: vsce package --no-dependencies --allow-missing-repository
      
    - name: Publish to VS Code Marketplace
      run: vsce publish -p ${{ secrets.AZURE_MARKETPLACE_TOKEN }} --no-dependencies --allow-missing-repository
